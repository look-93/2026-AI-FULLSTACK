<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.the703.dao.TestMapper"> <!-- Mapper's namespace cannot be empty -->
  <select id="now" resultType="string"> <!-- resultType 소문자 -->
  
	<!-- MYSQL 에서 날짜와 시간 SQL -->	
	select now();
	
  </select>
</mapper>